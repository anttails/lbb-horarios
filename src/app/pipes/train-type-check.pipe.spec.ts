import { TrainTypeCheckPipe } from './train-type-check.pipe';

describe('TrainTypeCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainTypeCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
